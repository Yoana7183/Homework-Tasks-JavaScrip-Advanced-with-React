// Write a JavaScript program to check two given numbers and 
// return true if one of the number is 50 or if their sum is 50.

function isFifty(a,b) {
  var isFifty;
    if(a===50||b===50||a+b===50){
    return isFifty = true
    }else{
       return isFifty=false
    }
}
console.log(isFifty(29,21));